/* tslint:disable */
/* eslint-disable */
/**
 * Typing API - version 1.0
 *
 * Typing应用的API文档
 *
 * OpenAPI version: 3.0.0
 *
 * Contact:
 *
 * NOTE: This file is auto generated by the alova's vscode plugin.
 *
 * https://alova.js.org/devtools/vscode
 *
 * **Do not edit the file manually.**
 */
import type {
  Alova,
  AlovaMethodCreateConfig,
  AlovaGenerics,
  Method
} from 'alova';
import type { $$userConfigMap, alovaInstance } from '.';
import type apiDefinitions from './apiDefinitions';

type CollapsedAlova = typeof alovaInstance;
type UserMethodConfigMap = typeof $$userConfigMap;

type Alova2MethodConfig<Responded> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Omit<
        AlovaMethodCreateConfig<
          AlovaGenerics<
            Responded,
            any,
            RequestConfig,
            Response,
            ResponseHeader,
            L1Cache,
            L2Cache,
            SE
          >,
          any,
          Responded
        >,
        'params'
      >
    : never;

// Extract the return type of transform function that define in $$userConfigMap, if it not exists, use the default type.
type ExtractUserDefinedTransformed<
  DefinitionKey extends keyof typeof apiDefinitions,
  Default
> = DefinitionKey extends keyof UserMethodConfigMap
  ? UserMethodConfigMap[DefinitionKey]['transform'] extends (
      ...args: any[]
    ) => any
    ? Awaited<ReturnType<UserMethodConfigMap[DefinitionKey]['transform']>>
    : Default
  : Default;
type Alova2Method<
  Responded,
  DefinitionKey extends keyof typeof apiDefinitions,
  CurrentConfig extends Alova2MethodConfig<any>
> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Method<
        AlovaGenerics<
          CurrentConfig extends undefined
            ? ExtractUserDefinedTransformed<DefinitionKey, Responded>
            : CurrentConfig['transform'] extends (...args: any[]) => any
              ? Awaited<ReturnType<CurrentConfig['transform']>>
              : ExtractUserDefinedTransformed<DefinitionKey, Responded>,
          any,
          RequestConfig,
          Response,
          ResponseHeader,
          L1Cache,
          L2Cache,
          SE
        >
      >
    : never;

export interface CreateUserDto {
  /**
   * 用户名
   */
  name: string;
  /**
   * 邮箱
   */
  email: string;
  /**
   * 密码
   */
  password: string;
  /**
   * 是否激活
   */
  isActive?: boolean;
  /**
   * 用户角色
   */
  role?: 'super_admin' | 'admin' | 'user' | 'guest';
}
export interface RegisterDto {
  /**
   * 用户名
   */
  name: string;
  /**
   * 邮箱
   */
  email: string;
  /**
   * 密码
   */
  password: string;
}
export interface LoginDto {
  /**
   * 邮箱
   */
  email: string;
  /**
   * 密码
   */
  password: string;
}
export interface CreateLanguageDto {
  /**
   * 语言名称
   */
  name: string;
  /**
   * 语言代码
   */
  code: string;
  /**
   * 文字体系
   */
  script: string;
  /**
   * 是否启用
   */
  isActive?: boolean;
}
export interface CreateCorpusCategoryDto {
  /**
   * 对应语言 ID
   */
  languageId: string;
  /**
   * 分类名称（旅游、商务、日常会话）
   */
  name: string;
  /**
   * 分类描述
   */
  description?: string;
  /**
   * 难度等级（1-5）
   */
  difficulty: number;
}
export interface CreateWordDto {
  /**
   * 所属语言 ID
   */
  languageId: string;
  /**
   * 所属分类 ID
   */
  categoryId: string;
  /**
   * 单词原文
   */
  word: string;
  /**
   * 罗马音/拼音
   */
  transliteration?: string;
  /**
   * 美式音标
   */
  usPhonetic?: string;
  /**
   * 英式音标
   */
  ukPhonetic?: string;
  /**
   * 中文释义
   */
  meaning: string;
  /**
   * 例句
   */
  example: string;
  /**
   * 发音音频链接
   */
  audioUrl?: string;
  /**
   * 图片链接
   */
  imageUrl?: string;
}
export interface CreateSentenceDto {
  /**
   * 所属语言 ID
   */
  languageId: string;
  /**
   * 所属分类 ID
   */
  categoryId: string;
  /**
   * 句子原文
   */
  sentence: string;
  /**
   * 中文释义
   */
  meaning: string;
  /**
   * 句子音频 URL
   */
  audioUrl?: string;
}
export interface ApiResponseDto {
  /**
   * 状态码
   */
  code: number;
  /**
   * 响应消息
   */
  message: string;
  /**
   * 响应数据
   */
  data: object;
  /**
   * 时间戳
   */
  timestamp: number;
  /**
   * 请求路径
   */
  path: string;
}
export interface User {
  /**
   * 用户ID
   */
  id: number;
  /**
   * 用户名
   */
  name: string;
  /**
   * 邮箱
   */
  email: string;
  /**
   * 密码
   */
  password: string;
  /**
   * 是否激活
   */
  isActive: boolean;
  /**
   * 用户角色
   */
  role: ('super_admin' | 'admin' | 'user' | 'guest') | null;
  /**
   * 用户状态
   */
  status: 'active' | 'disabled' | 'pending' | 'deleted';
  /**
   * 创建时间
   */
  createTime: string;
  /**
   * 更新时间
   */
  updateTime: string;
  /**
   * 删除时间
   */
  deleteTime?: string;
}
export interface Language {
  /**
   * 语言 ID
   */
  id: number;
  /**
   * 语言名称（English, 日本語, Français）
   */
  name: string;
  /**
   * 语言代码（en, ja, fr）
   */
  code: string;
  /**
   * 文字体系（Latin, Kanji, Cyrillic）
   */
  script: string;
  /**
   * 是否启用
   */
  isActive: boolean;
  /**
   * 创建时间
   */
  createTime: string;
  /**
   * 更新时间
   */
  updateTime: string;
  /**
   * 删除时间
   */
  deleteTime?: string;
}
export interface CorpusCategory {
  /**
   * 分类 ID
   */
  id: string;
  /**
   * 对应语言 ID
   */
  languageId: string;
  /**
   * 分类名称（旅游、商务、日常会话）
   */
  name: string;
  /**
   * 分类描述
   */
  description?: string;
  /**
   * 难度等级（1-5）
   */
  difficulty: number;
  /**
   * 创建时间
   */
  createTime: string;
  /**
   * 更新时间
   */
  updateTime: string;
  /**
   * 删除时间
   */
  deleteTime?: string;
  /**
   * 关联的语言
   */
  language: Language;
}
export type Object = object;
export interface PaginationResponseDto {
  /**
   * 状态码
   */
  code: number;
  /**
   * 响应消息
   */
  message: string;
  /**
   * 响应数据
   */
  data: object;
  /**
   * 时间戳
   */
  timestamp: number;
  /**
   * 请求路径
   */
  path: string;
}
export interface UserResponseDto {
  /**
   * 用户ID
   */
  id: number;
  /**
   * 用户名
   */
  name: string;
  /**
   * 邮箱
   */
  email: string;
  /**
   * 是否激活
   */
  isActive: boolean;
  /**
   * 用户角色
   */
  role: ('super_admin' | 'admin' | 'user' | 'guest') | null;
  /**
   * 用户状态
   */
  status: 'active' | 'disabled' | 'pending' | 'deleted';
  /**
   * 创建时间
   */
  createTime: string;
  /**
   * 更新时间
   */
  updateTime: string;
  /**
   * 删除时间
   */
  deleteTime?: string;
}
export interface AuthResponseDto {
  /**
   * 用户信息
   */
  user: UserResponseDto;
  /**
   * JWT访问令牌
   */
  accessToken: string;
}
export interface Word {
  /**
   * 单词 ID
   */
  id: string;
  /**
   * 所属语言 ID
   */
  languageId: string;
  /**
   * 所属分类 ID
   */
  categoryId: string;
  /**
   * 单词原文
   */
  word: string;
  /**
   * 罗马音/拼音
   */
  transliteration?: string;
  /**
   * 美式音标
   */
  usPhonetic?: string;
  /**
   * 英式音标
   */
  ukPhonetic?: string;
  /**
   * 中文释义
   */
  meaning: string;
  /**
   * 简短翻译
   */
  meaningShort: string;
  /**
   * 例句
   */
  example: string;
  /**
   * 发音音频链接
   */
  audioUrl?: string;
  /**
   * 图片链接
   */
  imageUrl?: string;
  /**
   * 创建时间
   */
  createdAt: string;
  /**
   * 更新时间
   */
  updatedAt: string;
  /**
   * 删除时间
   */
  deletedAt?: string;
  /**
   * 所属语言
   */
  language: Language;
  /**
   * 所属分类
   */
  category: CorpusCategory;
}
export interface Sentence {
  /**
   * 句子 ID
   */
  id: string;
  /**
   * 所属语言 ID
   */
  languageId: string;
  /**
   * 所属分类 ID
   */
  categoryId: string;
  /**
   * 句子原文
   */
  sentence: string;
  /**
   * 中文释义
   */
  meaning: string;
  /**
   * 句子音频 URL
   */
  audioUrl?: string;
  /**
   * 创建时间
   */
  createdAt: string;
  /**
   * 更新时间
   */
  updatedAt: string;
  /**
   * 删除时间
   */
  deletedAt?: string;
  /**
   * 所属语言
   */
  language: Language;
  /**
   * 所属分类
   */
  category: CorpusCategory;
}
declare global {
  interface Apis {
    general: {
      /**
       * ---
       *
       * [POST] 创建用户（仅管理员）
       *
       * **path:** /user
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 用户名
       *   name: string
       *   // 邮箱
       *   email: string
       *   // 密码
       *   password: string
       *   // 是否激活
       *   isActive?: boolean
       *   // 用户角色
       *   role?: 'super_admin' | 'admin' | 'user' | 'guest'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 用户ID
       *     id: number
       *     // 用户名
       *     name: string
       *     // 邮箱
       *     email: string
       *     // 密码
       *     password: string
       *     // 是否激活
       *     isActive: boolean
       *     // 用户角色
       *     role: ('super_admin' | 'admin' | 'user' | 'guest') | null
       *     // 用户状态
       *     status: 'active' | 'disabled' | 'pending' | 'deleted'
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       * }
       * ```
       */
      UserController_create<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: User;
          }
        > & {
          data: CreateUserDto;
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: User;
        },
        'general.UserController_create',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页查询用户（仅管理员）
       *
       * **path:** /user/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 用户ID
       *       id: number
       *       // 用户名
       *       name: string
       *       // 邮箱
       *       email: string
       *       // 密码
       *       password: string
       *       // 是否激活
       *       isActive: boolean
       *       // 用户角色
       *       role: ('super_admin' | 'admin' | 'user' | 'guest') | null
       *       // 用户状态
       *       status: 'active' | 'disabled' | 'pending' | 'deleted'
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }>
       *   }
       * }
       * ```
       */
      UserController_findPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: User[];
            };
          }
        > & {
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: User[];
          };
        },
        'general.UserController_findPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据用户 Token 查询当前用户
       *
       * **path:** /user/me
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 用户ID
       *     id: number
       *     // 用户名
       *     name: string
       *     // 邮箱
       *     email: string
       *     // 密码
       *     password: string
       *     // 是否激活
       *     isActive: boolean
       *     // 用户角色
       *     role: ('super_admin' | 'admin' | 'user' | 'guest') | null
       *     // 用户状态
       *     status: 'active' | 'disabled' | 'pending' | 'deleted'
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       * }
       * ```
       */
      UserController_findMe<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: User;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: User;
        },
        'general.UserController_findMe',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据ID查询用户
       *
       * **path:** /user/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用户ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 用户ID
       *     id: number
       *     // 用户名
       *     name: string
       *     // 邮箱
       *     email: string
       *     // 密码
       *     password: string
       *     // 是否激活
       *     isActive: boolean
       *     // 用户角色
       *     role: ('super_admin' | 'admin' | 'user' | 'guest') | null
       *     // 用户状态
       *     status: 'active' | 'disabled' | 'pending' | 'deleted'
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       * }
       * ```
       */
      UserController_findOne<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: User;
          }
        > & {
          pathParams: {
            /**
             * 用户ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: User;
        },
        'general.UserController_findOne',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 根据ID删除用户（仅超级管理员）
       *
       * **path:** /user/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用户ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * }
       * ```
       */
      UserController_remove<
        Config extends Alova2MethodConfig<ApiResponseDto> & {
          pathParams: {
            /**
             * 用户ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ApiResponseDto, 'general.UserController_remove', Config>;
      /**
       * ---
       *
       * [POST] 用户注册
       *
       * **path:** /auth/register
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 用户名
       *   name: string
       *   // 邮箱
       *   email: string
       *   // 密码
       *   password: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 用户信息
       *   user: {
       *     // 用户ID
       *     id: number
       *     // 用户名
       *     name: string
       *     // 邮箱
       *     email: string
       *     // 是否激活
       *     isActive: boolean
       *     // 用户角色
       *     role: ('super_admin' | 'admin' | 'user' | 'guest') | null
       *     // 用户状态
       *     status: 'active' | 'disabled' | 'pending' | 'deleted'
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       *   // JWT访问令牌
       *   accessToken: string
       * }
       * ```
       */
      AuthController_register<
        Config extends Alova2MethodConfig<AuthResponseDto> & {
          data: RegisterDto;
        }
      >(
        config: Config
      ): Alova2Method<
        AuthResponseDto,
        'general.AuthController_register',
        Config
      >;
      /**
       * ---
       *
       * [POST] 用户登录
       *
       * **path:** /auth/login
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 邮箱
       *   email: string
       *   // 密码
       *   password: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 用户信息
       *   user: {
       *     // 用户ID
       *     id: number
       *     // 用户名
       *     name: string
       *     // 邮箱
       *     email: string
       *     // 是否激活
       *     isActive: boolean
       *     // 用户角色
       *     role: ('super_admin' | 'admin' | 'user' | 'guest') | null
       *     // 用户状态
       *     status: 'active' | 'disabled' | 'pending' | 'deleted'
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       *   // JWT访问令牌
       *   accessToken: string
       * }
       * ```
       */
      AuthController_login<
        Config extends Alova2MethodConfig<AuthResponseDto> & {
          data: LoginDto;
        }
      >(
        config: Config
      ): Alova2Method<AuthResponseDto, 'general.AuthController_login', Config>;
      /**
       * ---
       *
       * [GET] GitHub OAuth 登录
       *
       * **path:** /auth/github
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      AuthController_githubAuth<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'general.AuthController_githubAuth', Config>;
      /**
       * ---
       *
       * [GET] GitHub OAuth 回调
       *
       * **path:** /auth/github/callback
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      AuthController_githubCallback<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'general.AuthController_githubCallback', Config>;
      /**
       * ---
       *
       * [GET] QQ OAuth 登录
       *
       * **path:** /auth/qq
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      AuthController_qqAuth<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'general.AuthController_qqAuth', Config>;
      /**
       * ---
       *
       * [GET] QQ OAuth 回调
       *
       * **path:** /auth/qq/callback
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      AuthController_qqCallback<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'general.AuthController_qqCallback', Config>;
      /**
       * ---
       *
       * [GET] 获取用户信息
       *
       * **path:** /auth/profile
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      AuthController_getProfile<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'general.AuthController_getProfile', Config>;
      /**
       * ---
       *
       * [GET] 绑定GitHub账户 - 重定向到GitHub授权页面
       *
       * **path:** /auth/bind/github
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      AuthController_bindGithub<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'general.AuthController_bindGithub', Config>;
      /**
       * ---
       *
       * [POST] 手动绑定GitHub账户
       *
       * **path:** /auth/bind/github/manual
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      AuthController_bindGithubManual<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'general.AuthController_bindGithubManual', Config>;
      /**
       * ---
       *
       * [POST] 解绑GitHub账户
       *
       * **path:** /auth/unbind/github
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      AuthController_unbindGithub<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'general.AuthController_unbindGithub', Config>;
      /**
       * ---
       *
       * [GET] 绑定QQ账户 - 重定向到QQ授权页面
       *
       * **path:** /auth/bind/qq
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      AuthController_bindQQ<Config extends Alova2MethodConfig<unknown>>(
        config?: Config
      ): Alova2Method<unknown, 'general.AuthController_bindQQ', Config>;
      /**
       * ---
       *
       * [POST] 手动绑定QQ账户
       *
       * **path:** /auth/bind/qq/manual
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      AuthController_bindQQManual<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'general.AuthController_bindQQManual', Config>;
      /**
       * ---
       *
       * [POST] 解绑QQ账户
       *
       * **path:** /auth/unbind/qq
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      AuthController_unbindQQ<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'general.AuthController_unbindQQ', Config>;
      /**
       * ---
       *
       * [GET] 获取用户绑定的第三方账户
       *
       * **path:** /auth/bindings
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      AuthController_getBindings<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'general.AuthController_getBindings', Config>;
      /**
       * ---
       *
       * [POST] 创建语言
       *
       * **path:** /languages
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 语言名称
       *   name: string
       *   // 语言代码
       *   code: string
       *   // 文字体系
       *   script: string
       *   // 是否启用
       *   isActive?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 语言 ID
       *     id: number
       *     // 语言名称（English, 日本語, Français）
       *     name: string
       *     // 语言代码（en, ja, fr）
       *     code: string
       *     // 文字体系（Latin, Kanji, Cyrillic）
       *     script: string
       *     // 是否启用
       *     isActive: boolean
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       * }
       * ```
       */
      LanguagesController_create<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Language;
          }
        > & {
          data: CreateLanguageDto;
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Language;
        },
        'general.LanguagesController_create',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取所有语言列表
       *
       * **path:** /languages
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 语言 ID
       *     id: number
       *     // 语言名称（English, 日本語, Français）
       *     name: string
       *     // 语言代码（en, ja, fr）
       *     code: string
       *     // 文字体系（Latin, Kanji, Cyrillic）
       *     script: string
       *     // 是否启用
       *     isActive: boolean
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }>
       * }
       * ```
       */
      LanguagesController_findAll<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Language[];
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Language[];
        },
        'general.LanguagesController_findAll',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取所有启用的语言列表
       *
       * **path:** /languages/active
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 语言 ID
       *     id: number
       *     // 语言名称（English, 日本語, Français）
       *     name: string
       *     // 语言代码（en, ja, fr）
       *     code: string
       *     // 文字体系（Latin, Kanji, Cyrillic）
       *     script: string
       *     // 是否启用
       *     isActive: boolean
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }>
       * }
       * ```
       */
      LanguagesController_findAllActive<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Language[];
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Language[];
        },
        'general.LanguagesController_findAllActive',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页查询语言（仅管理员）
       *
       * **path:** /languages/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }>
       *   }
       * }
       * ```
       */
      LanguagesController_findAllPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Language[];
            };
          }
        > & {
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Language[];
          };
        },
        'general.LanguagesController_findAllPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言代码查询语言
       *
       * **path:** /languages/code/{code}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言代码
       *   code: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 语言 ID
       *     id: number
       *     // 语言名称（English, 日本語, Français）
       *     name: string
       *     // 语言代码（en, ja, fr）
       *     code: string
       *     // 文字体系（Latin, Kanji, Cyrillic）
       *     script: string
       *     // 是否启用
       *     isActive: boolean
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       * }
       * ```
       */
      LanguagesController_findByCode<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Language;
          }
        > & {
          pathParams: {
            /**
             * 语言代码
             */
            code: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Language;
        },
        'general.LanguagesController_findByCode',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据ID查询语言
       *
       * **path:** /languages/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 语言 ID
       *     id: number
       *     // 语言名称（English, 日本語, Français）
       *     name: string
       *     // 语言代码（en, ja, fr）
       *     code: string
       *     // 文字体系（Latin, Kanji, Cyrillic）
       *     script: string
       *     // 是否启用
       *     isActive: boolean
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *   }
       * }
       * ```
       */
      LanguagesController_findOne<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Language;
          }
        > & {
          pathParams: {
            /**
             * 语言ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Language;
        },
        'general.LanguagesController_findOne',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除语言
       *
       * **path:** /languages/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * }
       * ```
       */
      LanguagesController_remove<
        Config extends Alova2MethodConfig<ApiResponseDto> & {
          pathParams: {
            /**
             * 语言ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto,
        'general.LanguagesController_remove',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建语料库分类（仅管理员）
       *
       * **path:** /corpus-categories
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 对应语言 ID
       *   languageId: string
       *   // 分类名称（旅游、商务、日常会话）
       *   name: string
       *   // 分类描述
       *   description?: string
       *   // 难度等级（1-5）
       *   difficulty: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 分类 ID
       *     id: string
       *     // 对应语言 ID
       *     languageId: string
       *     // 分类名称（旅游、商务、日常会话）
       *     name: string
       *     // 分类描述
       *     description?: string
       *     // 难度等级（1-5）
       *     difficulty: number
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *     // 关联的语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *   }
       * }
       * ```
       */
      CorpusCategoriesController_create<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: CorpusCategory;
          }
        > & {
          data: CreateCorpusCategoryDto;
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: CorpusCategory;
        },
        'general.CorpusCategoriesController_create',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取所有语料库分类
       *
       * **path:** /corpus-categories
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 分类 ID
       *     id: string
       *     // 对应语言 ID
       *     languageId: string
       *     // 分类名称（旅游、商务、日常会话）
       *     name: string
       *     // 分类描述
       *     description?: string
       *     // 难度等级（1-5）
       *     difficulty: number
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *     // 关联的语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *   }>
       * }
       * ```
       */
      CorpusCategoriesController_findAll<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: CorpusCategory[];
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: CorpusCategory[];
        },
        'general.CorpusCategoriesController_findAll',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页查询语料库分类（仅管理员）
       *
       * **path:** /corpus-categories/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      CorpusCategoriesController_findAllPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: CorpusCategory[];
            };
          }
        > & {
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: CorpusCategory[];
          };
        },
        'general.CorpusCategoriesController_findAllPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言 ID 查询分类
       *
       * **path:** /corpus-categories/language/{languageId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言 ID
       *   languageId: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 分类 ID
       *     id: string
       *     // 对应语言 ID
       *     languageId: string
       *     // 分类名称（旅游、商务、日常会话）
       *     name: string
       *     // 分类描述
       *     description?: string
       *     // 难度等级（1-5）
       *     difficulty: number
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *     // 关联的语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *   }>
       * }
       * ```
       */
      CorpusCategoriesController_findByLanguageId<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: CorpusCategory[];
          }
        > & {
          pathParams: {
            /**
             * 语言 ID
             */
            languageId: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: CorpusCategory[];
        },
        'general.CorpusCategoriesController_findByLanguageId',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据难度等级查询分类
       *
       * **path:** /corpus-categories/difficulty/{difficulty}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 难度等级（1-5）
       *   difficulty: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 分类 ID
       *     id: string
       *     // 对应语言 ID
       *     languageId: string
       *     // 分类名称（旅游、商务、日常会话）
       *     name: string
       *     // 分类描述
       *     description?: string
       *     // 难度等级（1-5）
       *     difficulty: number
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *     // 关联的语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *   }>
       * }
       * ```
       */
      CorpusCategoriesController_findByDifficulty<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: CorpusCategory[];
          }
        > & {
          pathParams: {
            /**
             * 难度等级（1-5）
             */
            difficulty: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: CorpusCategory[];
        },
        'general.CorpusCategoriesController_findByDifficulty',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言 ID 和难度等级查询分类
       *
       * **path:** /corpus-categories/language/{languageId}/difficulty/{difficulty}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言 ID
       *   languageId: string
       *   // 难度等级（1-5）
       *   difficulty: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 分类 ID
       *     id: string
       *     // 对应语言 ID
       *     languageId: string
       *     // 分类名称（旅游、商务、日常会话）
       *     name: string
       *     // 分类描述
       *     description?: string
       *     // 难度等级（1-5）
       *     difficulty: number
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *     // 关联的语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *   }>
       * }
       * ```
       */
      CorpusCategoriesController_findByLanguageAndDifficulty<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: CorpusCategory[];
          }
        > & {
          pathParams: {
            /**
             * 语言 ID
             */
            languageId: string;
            /**
             * 难度等级（1-5）
             */
            difficulty: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: CorpusCategory[];
        },
        'general.CorpusCategoriesController_findByLanguageAndDifficulty',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取难度等级统计（仅管理员）
       *
       * **path:** /corpus-categories/stats/difficulty
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * }
       * ```
       */
      CorpusCategoriesController_getDifficultyStats<
        Config extends Alova2MethodConfig<ApiResponseDto>
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto,
        'general.CorpusCategoriesController_getDifficultyStats',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取语言分类数量统计（仅管理员）
       *
       * **path:** /corpus-categories/stats/language
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * }
       * ```
       */
      CorpusCategoriesController_getLanguageStats<
        Config extends Alova2MethodConfig<ApiResponseDto>
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto,
        'general.CorpusCategoriesController_getLanguageStats',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据 ID 查询语料库分类
       *
       * **path:** /corpus-categories/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 分类 ID
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 分类 ID
       *     id: string
       *     // 对应语言 ID
       *     languageId: string
       *     // 分类名称（旅游、商务、日常会话）
       *     name: string
       *     // 分类描述
       *     description?: string
       *     // 难度等级（1-5）
       *     difficulty: number
       *     // 创建时间
       *     createTime: string
       *     // 更新时间
       *     updateTime: string
       *     // 删除时间
       *     deleteTime?: string
       *     // 关联的语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *   }
       * }
       * ```
       */
      CorpusCategoriesController_findOne<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: CorpusCategory;
          }
        > & {
          pathParams: {
            /**
             * 分类 ID
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: CorpusCategory;
        },
        'general.CorpusCategoriesController_findOne',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除语料库分类（仅管理员）
       *
       * **path:** /corpus-categories/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 分类 ID
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * }
       * ```
       */
      CorpusCategoriesController_remove<
        Config extends Alova2MethodConfig<ApiResponseDto> & {
          pathParams: {
            /**
             * 分类 ID
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto,
        'general.CorpusCategoriesController_remove',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建单词（仅管理员）
       *
       * **path:** /words
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 所属语言 ID
       *   languageId: string
       *   // 所属分类 ID
       *   categoryId: string
       *   // 单词原文
       *   word: string
       *   // 罗马音/拼音
       *   transliteration?: string
       *   // 美式音标
       *   usPhonetic?: string
       *   // 英式音标
       *   ukPhonetic?: string
       *   // 中文释义
       *   meaning: string
       *   // 例句
       *   example: string
       *   // 发音音频链接
       *   audioUrl?: string
       *   // 图片链接
       *   imageUrl?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 单词 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 单词原文
       *     word: string
       *     // 罗马音/拼音
       *     transliteration?: string
       *     // 美式音标
       *     usPhonetic?: string
       *     // 英式音标
       *     ukPhonetic?: string
       *     // 中文释义
       *     meaning: string
       *     // 简短翻译
       *     meaningShort: string
       *     // 例句
       *     example: string
       *     // 发音音频链接
       *     audioUrl?: string
       *     // 图片链接
       *     imageUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }
       * }
       * ```
       */
      WordsController_create<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Word;
          }
        > & {
          data: CreateWordDto;
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Word;
        },
        'general.WordsController_create',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页查询单词（仅管理员）
       *
       * **path:** /words/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 单词 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 单词原文
       *       word: string
       *       // 罗马音/拼音
       *       transliteration?: string
       *       // 美式音标
       *       usPhonetic?: string
       *       // 英式音标
       *       ukPhonetic?: string
       *       // 中文释义
       *       meaning: string
       *       // 简短翻译
       *       meaningShort: string
       *       // 例句
       *       example: string
       *       // 发音音频链接
       *       audioUrl?: string
       *       // 图片链接
       *       imageUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      WordsController_findAllPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Word[];
            };
          }
        > & {
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Word[];
          };
        },
        'general.WordsController_findAllPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言 ID 查询单词
       *
       * **path:** /words/language/{languageId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言 ID
       *   languageId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 单词 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 单词原文
       *       word: string
       *       // 罗马音/拼音
       *       transliteration?: string
       *       // 美式音标
       *       usPhonetic?: string
       *       // 英式音标
       *       ukPhonetic?: string
       *       // 中文释义
       *       meaning: string
       *       // 简短翻译
       *       meaningShort: string
       *       // 例句
       *       example: string
       *       // 发音音频链接
       *       audioUrl?: string
       *       // 图片链接
       *       imageUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      WordsController_findByLanguageId<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Word[];
            };
          }
        > & {
          pathParams: {
            /**
             * 语言 ID
             */
            languageId: string;
          };
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Word[];
          };
        },
        'general.WordsController_findByLanguageId',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据分类 ID 查询单词
       *
       * **path:** /words/category/{categoryId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 分类 ID
       *   categoryId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 单词 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 单词原文
       *       word: string
       *       // 罗马音/拼音
       *       transliteration?: string
       *       // 美式音标
       *       usPhonetic?: string
       *       // 英式音标
       *       ukPhonetic?: string
       *       // 中文释义
       *       meaning: string
       *       // 简短翻译
       *       meaningShort: string
       *       // 例句
       *       example: string
       *       // 发音音频链接
       *       audioUrl?: string
       *       // 图片链接
       *       imageUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      WordsController_findByCategoryId<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Word[];
            };
          }
        > & {
          pathParams: {
            /**
             * 分类 ID
             */
            categoryId: string;
          };
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Word[];
          };
        },
        'general.WordsController_findByCategoryId',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言和分类查询单词
       *
       * **path:** /words/language/{languageId}/category/{categoryId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言 ID
       *   languageId: string
       *   // 分类 ID
       *   categoryId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 单词 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 单词原文
       *       word: string
       *       // 罗马音/拼音
       *       transliteration?: string
       *       // 美式音标
       *       usPhonetic?: string
       *       // 英式音标
       *       ukPhonetic?: string
       *       // 中文释义
       *       meaning: string
       *       // 简短翻译
       *       meaningShort: string
       *       // 例句
       *       example: string
       *       // 发音音频链接
       *       audioUrl?: string
       *       // 图片链接
       *       imageUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      WordsController_findByLanguageAndCategory<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Word[];
            };
          }
        > & {
          pathParams: {
            /**
             * 语言 ID
             */
            languageId: string;
            /**
             * 分类 ID
             */
            categoryId: string;
          };
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Word[];
          };
        },
        'general.WordsController_findByLanguageAndCategory',
        Config
      >;
      /**
       * ---
       *
       * [GET] 搜索单词
       *
       * **path:** /words/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 搜索关键词
       *   keyword: string
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       *   // 语言 ID
       *   languageId?: string
       *   // 分类 ID
       *   categoryId?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 单词 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 单词原文
       *     word: string
       *     // 罗马音/拼音
       *     transliteration?: string
       *     // 美式音标
       *     usPhonetic?: string
       *     // 英式音标
       *     ukPhonetic?: string
       *     // 中文释义
       *     meaning: string
       *     // 简短翻译
       *     meaningShort: string
       *     // 例句
       *     example: string
       *     // 发音音频链接
       *     audioUrl?: string
       *     // 图片链接
       *     imageUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }>
       * }
       * ```
       */
      WordsController_searchWords<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Word[];
          }
        > & {
          params: {
            /**
             * 搜索关键词
             */
            keyword: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
            /**
             * 语言 ID
             */
            languageId?: string;
            /**
             * 分类 ID
             */
            categoryId?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Word[];
        },
        'general.WordsController_searchWords',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页搜索单词
       *
       * **path:** /words/search/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 搜索关键词
       *   keyword: string
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       *   // 语言 ID
       *   languageId?: string
       *   // 分类 ID
       *   categoryId?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 单词 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 单词原文
       *       word: string
       *       // 罗马音/拼音
       *       transliteration?: string
       *       // 美式音标
       *       usPhonetic?: string
       *       // 英式音标
       *       ukPhonetic?: string
       *       // 中文释义
       *       meaning: string
       *       // 简短翻译
       *       meaningShort: string
       *       // 例句
       *       example: string
       *       // 发音音频链接
       *       audioUrl?: string
       *       // 图片链接
       *       imageUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      WordsController_searchWordsPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Word[];
            };
          }
        > & {
          params: {
            /**
             * 搜索关键词
             */
            keyword: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
            /**
             * 语言 ID
             */
            languageId?: string;
            /**
             * 分类 ID
             */
            categoryId?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Word[];
          };
        },
        'general.WordsController_searchWordsPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 随机获取单词（用于练习）
       *
       * **path:** /words/random
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 单词数量
       *   count?: number
       *   // 语言 ID
       *   languageId?: string
       *   // 分类 ID
       *   categoryId?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 单词 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 单词原文
       *     word: string
       *     // 罗马音/拼音
       *     transliteration?: string
       *     // 美式音标
       *     usPhonetic?: string
       *     // 英式音标
       *     ukPhonetic?: string
       *     // 中文释义
       *     meaning: string
       *     // 简短翻译
       *     meaningShort: string
       *     // 例句
       *     example: string
       *     // 发音音频链接
       *     audioUrl?: string
       *     // 图片链接
       *     imageUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }>
       * }
       * ```
       */
      WordsController_getRandomWords<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Word[];
          }
        > & {
          params: {
            /**
             * 单词数量
             */
            count?: number;
            /**
             * 语言 ID
             */
            languageId?: string;
            /**
             * 分类 ID
             */
            categoryId?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Word[];
        },
        'general.WordsController_getRandomWords',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取语言统计信息（仅管理员）
       *
       * **path:** /words/stats/language
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: object
       * }
       * ```
       */
      WordsController_getLanguageStats<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Object;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Object;
        },
        'general.WordsController_getLanguageStats',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取分类统计信息（仅管理员）
       *
       * **path:** /words/stats/category
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: object
       * }
       * ```
       */
      WordsController_getCategoryStats<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Object;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Object;
        },
        'general.WordsController_getCategoryStats',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据 ID 查询单词详情
       *
       * **path:** /words/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 单词 ID
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 单词 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 单词原文
       *     word: string
       *     // 罗马音/拼音
       *     transliteration?: string
       *     // 美式音标
       *     usPhonetic?: string
       *     // 英式音标
       *     ukPhonetic?: string
       *     // 中文释义
       *     meaning: string
       *     // 简短翻译
       *     meaningShort: string
       *     // 例句
       *     example: string
       *     // 发音音频链接
       *     audioUrl?: string
       *     // 图片链接
       *     imageUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }
       * }
       * ```
       */
      WordsController_findOne<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Word;
          }
        > & {
          pathParams: {
            /**
             * 单词 ID
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Word;
        },
        'general.WordsController_findOne',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除单词（仅管理员）
       *
       * **path:** /words/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 单词 ID
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: object
       * }
       * ```
       */
      WordsController_remove<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Object;
          }
        > & {
          pathParams: {
            /**
             * 单词 ID
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Object;
        },
        'general.WordsController_remove',
        Config
      >;
      /**
       * ---
       *
       * [POST] 创建句子（仅管理员）
       *
       * **path:** /sentences
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // 所属语言 ID
       *   languageId: string
       *   // 所属分类 ID
       *   categoryId: string
       *   // 句子原文
       *   sentence: string
       *   // 中文释义
       *   meaning: string
       *   // 句子音频 URL
       *   audioUrl?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 句子 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 句子原文
       *     sentence: string
       *     // 中文释义
       *     meaning: string
       *     // 句子音频 URL
       *     audioUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }
       * }
       * ```
       */
      SentencesController_create<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Sentence;
          }
        > & {
          data: CreateSentenceDto;
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Sentence;
        },
        'general.SentencesController_create',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页查询句子（仅管理员）
       *
       * **path:** /sentences/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 句子 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 句子原文
       *       sentence: string
       *       // 中文释义
       *       meaning: string
       *       // 句子音频 URL
       *       audioUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      SentencesController_findAllPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Sentence[];
            };
          }
        > & {
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Sentence[];
          };
        },
        'general.SentencesController_findAllPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言 ID 查询句子
       *
       * **path:** /sentences/language/{languageId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言 ID
       *   languageId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 句子 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 句子原文
       *       sentence: string
       *       // 中文释义
       *       meaning: string
       *       // 句子音频 URL
       *       audioUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      SentencesController_findByLanguageId<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Sentence[];
            };
          }
        > & {
          pathParams: {
            /**
             * 语言 ID
             */
            languageId: string;
          };
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Sentence[];
          };
        },
        'general.SentencesController_findByLanguageId',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据分类 ID 查询句子
       *
       * **path:** /sentences/category/{categoryId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 分类 ID
       *   categoryId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 句子 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 句子原文
       *       sentence: string
       *       // 中文释义
       *       meaning: string
       *       // 句子音频 URL
       *       audioUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      SentencesController_findByCategoryId<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Sentence[];
            };
          }
        > & {
          pathParams: {
            /**
             * 分类 ID
             */
            categoryId: string;
          };
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Sentence[];
          };
        },
        'general.SentencesController_findByCategoryId',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据语言 ID 和分类 ID 查询句子
       *
       * **path:** /sentences/language/{languageId}/category/{categoryId}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 语言 ID
       *   languageId: string
       *   // 分类 ID
       *   categoryId: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 句子 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 句子原文
       *       sentence: string
       *       // 中文释义
       *       meaning: string
       *       // 句子音频 URL
       *       audioUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      SentencesController_findByLanguageAndCategory<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Sentence[];
            };
          }
        > & {
          pathParams: {
            /**
             * 语言 ID
             */
            languageId: string;
            /**
             * 分类 ID
             */
            categoryId: string;
          };
          params: {
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Sentence[];
          };
        },
        'general.SentencesController_findByLanguageAndCategory',
        Config
      >;
      /**
       * ---
       *
       * [GET] 搜索句子
       *
       * **path:** /sentences/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 搜索关键词
       *   keyword: string
       *   // 语言 ID
       *   languageId?: string
       *   // 分类 ID
       *   categoryId?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 句子 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 句子原文
       *     sentence: string
       *     // 中文释义
       *     meaning: string
       *     // 句子音频 URL
       *     audioUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }>
       * }
       * ```
       */
      SentencesController_searchSentences<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Sentence[];
          }
        > & {
          params: {
            /**
             * 搜索关键词
             */
            keyword: string;
            /**
             * 语言 ID
             */
            languageId?: string;
            /**
             * 分类 ID
             */
            categoryId?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Sentence[];
        },
        'general.SentencesController_searchSentences',
        Config
      >;
      /**
       * ---
       *
       * [GET] 分页搜索句子
       *
       * **path:** /sentences/search/paginated
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 搜索关键词
       *   keyword: string
       *   // 页码
       *   page?: number
       *   // 每页数量
       *   pageSize?: number
       *   // 语言 ID
       *   languageId?: string
       *   // 分类 ID
       *   categoryId?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // [items] start
       *     // [items] end
       *     list?: Array<{
       *       // 句子 ID
       *       id: string
       *       // 所属语言 ID
       *       languageId: string
       *       // 所属分类 ID
       *       categoryId: string
       *       // 句子原文
       *       sentence: string
       *       // 中文释义
       *       meaning: string
       *       // 句子音频 URL
       *       audioUrl?: string
       *       // 创建时间
       *       createdAt: string
       *       // 更新时间
       *       updatedAt: string
       *       // 删除时间
       *       deletedAt?: string
       *       // 所属语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *       // 所属分类
       *       category: {
       *         // 分类 ID
       *         id: string
       *         // 对应语言 ID
       *         languageId: string
       *         // 分类名称（旅游、商务、日常会话）
       *         name: string
       *         // 分类描述
       *         description?: string
       *         // 难度等级（1-5）
       *         difficulty: number
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *         // 关联的语言
       *         language: {
       *           // 语言 ID
       *           id: number
       *           // 语言名称（English, 日本語, Français）
       *           name: string
       *           // 语言代码（en, ja, fr）
       *           code: string
       *           // 文字体系（Latin, Kanji, Cyrillic）
       *           script: string
       *           // 是否启用
       *           isActive: boolean
       *           // 创建时间
       *           createTime: string
       *           // 更新时间
       *           updateTime: string
       *           // 删除时间
       *           deleteTime?: string
       *         }
       *       }
       *     }>
       *   }
       * }
       * ```
       */
      SentencesController_searchSentencesPaginated<
        Config extends Alova2MethodConfig<
          PaginationResponseDto & {
            data?: {
              list?: Sentence[];
            };
          }
        > & {
          params: {
            /**
             * 搜索关键词
             */
            keyword: string;
            /**
             * 页码
             */
            page?: number;
            /**
             * 每页数量
             */
            pageSize?: number;
            /**
             * 语言 ID
             */
            languageId?: string;
            /**
             * 分类 ID
             */
            categoryId?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        PaginationResponseDto & {
          data?: {
            list?: Sentence[];
          };
        },
        'general.SentencesController_searchSentencesPaginated',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取随机句子
       *
       * **path:** /sentences/random
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 数量
       *   count?: number
       *   // 语言 ID
       *   languageId?: string
       *   // 分类 ID
       *   categoryId?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   // [items] start
       *   // [items] end
       *   data?: Array<{
       *     // 句子 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 句子原文
       *     sentence: string
       *     // 中文释义
       *     meaning: string
       *     // 句子音频 URL
       *     audioUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }>
       * }
       * ```
       */
      SentencesController_getRandomSentences<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Sentence[];
          }
        > & {
          params: {
            /**
             * 数量
             */
            count?: number;
            /**
             * 语言 ID
             */
            languageId?: string;
            /**
             * 分类 ID
             */
            categoryId?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Sentence[];
        },
        'general.SentencesController_getRandomSentences',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取语言统计信息（仅管理员）
       *
       * **path:** /sentences/stats/language
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: object
       * }
       * ```
       */
      SentencesController_getLanguageStats<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Object;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Object;
        },
        'general.SentencesController_getLanguageStats',
        Config
      >;
      /**
       * ---
       *
       * [GET] 获取分类统计信息（仅管理员）
       *
       * **path:** /sentences/stats/category
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: object
       * }
       * ```
       */
      SentencesController_getCategoryStats<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Object;
          }
        >
      >(
        config?: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Object;
        },
        'general.SentencesController_getCategoryStats',
        Config
      >;
      /**
       * ---
       *
       * [GET] 根据 ID 查询句子详情
       *
       * **path:** /sentences/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 句子 ID
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: {
       *     // 句子 ID
       *     id: string
       *     // 所属语言 ID
       *     languageId: string
       *     // 所属分类 ID
       *     categoryId: string
       *     // 句子原文
       *     sentence: string
       *     // 中文释义
       *     meaning: string
       *     // 句子音频 URL
       *     audioUrl?: string
       *     // 创建时间
       *     createdAt: string
       *     // 更新时间
       *     updatedAt: string
       *     // 删除时间
       *     deletedAt?: string
       *     // 所属语言
       *     language: {
       *       // 语言 ID
       *       id: number
       *       // 语言名称（English, 日本語, Français）
       *       name: string
       *       // 语言代码（en, ja, fr）
       *       code: string
       *       // 文字体系（Latin, Kanji, Cyrillic）
       *       script: string
       *       // 是否启用
       *       isActive: boolean
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *     }
       *     // 所属分类
       *     category: {
       *       // 分类 ID
       *       id: string
       *       // 对应语言 ID
       *       languageId: string
       *       // 分类名称（旅游、商务、日常会话）
       *       name: string
       *       // 分类描述
       *       description?: string
       *       // 难度等级（1-5）
       *       difficulty: number
       *       // 创建时间
       *       createTime: string
       *       // 更新时间
       *       updateTime: string
       *       // 删除时间
       *       deleteTime?: string
       *       // 关联的语言
       *       language: {
       *         // 语言 ID
       *         id: number
       *         // 语言名称（English, 日本語, Français）
       *         name: string
       *         // 语言代码（en, ja, fr）
       *         code: string
       *         // 文字体系（Latin, Kanji, Cyrillic）
       *         script: string
       *         // 是否启用
       *         isActive: boolean
       *         // 创建时间
       *         createTime: string
       *         // 更新时间
       *         updateTime: string
       *         // 删除时间
       *         deleteTime?: string
       *       }
       *     }
       *   }
       * }
       * ```
       */
      SentencesController_findOne<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Sentence;
          }
        > & {
          pathParams: {
            /**
             * 句子 ID
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Sentence;
        },
        'general.SentencesController_findOne',
        Config
      >;
      /**
       * ---
       *
       * [DELETE] 删除句子（仅管理员）
       *
       * **path:** /sentences/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 句子 ID
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 状态码
       *   code: number
       *   // 响应消息
       *   message: string
       *   // 响应数据
       *   data: object
       *   // 时间戳
       *   timestamp: number
       *   // 请求路径
       *   path: string
       * } & {
       *   data?: object
       * }
       * ```
       */
      SentencesController_remove<
        Config extends Alova2MethodConfig<
          ApiResponseDto & {
            data?: Object;
          }
        > & {
          pathParams: {
            /**
             * 句子 ID
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        ApiResponseDto & {
          data?: Object;
        },
        'general.SentencesController_remove',
        Config
      >;
    };
    Speech: {
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /speech/audio
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   input: string
       *   voice: string
       *   language: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      SpeechController_getText2Speech<
        Config extends Alova2MethodConfig<null> & {
          params: {
            input: string;
            voice: string;
            language: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'Speech.SpeechController_getText2Speech', Config>;
    };
  }

  var Apis: Apis;
}
